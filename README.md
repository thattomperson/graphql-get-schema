```
Usage: cli [options]

Options:
  -u, --url <url>        http://localhost:3000/graphql
  -p, --path <path>      ./schema.graphql
  -k, --insecure
  -c, --cookie <cookie>  Custom cookie string to send with the request
  -h, --help             display help for command
```